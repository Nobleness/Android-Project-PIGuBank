<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo">
    </style>
    
    <style name="ActivityTheme" parent="AppTheme">
        <item name="android:textSize">@dimen/textSize</item>
        <item name="android:typeface">serif</item>
        <item name="android:background">@color/backgrnd</item>
        
    </style>
    
    <style name="TitleFontStyle" parent="ActivityTheme">
        <item name="android:textSize">@dimen/titleTextSize</item>
        <item name="android:textColor">@color/textOverBackgrnd</item>
        <item name="android:textStyle">bold</item>
    </style>
    
    <style name="SmallTitleFontStyle" parent="ActivityTheme">
        <item name="android:textColor">@color/textOverBackgrnd</item>
        <item name="android:textStyle">bold</item>
    </style>
    
    <style name="GradientFontStyle" parent="AppTheme">
        <item name="android:textColor">@color/textOverGradient</item>
        <item name="android:textSize">@dimen/titleTextSize</item>
        <item name="android:typeface">serif</item>
        <item name="android:textStyle">bold</item>
    </style> 
    
    <style name="CustomAlertDialog" parent="@android:style/Theme.Holo.Dialog">
        <item name="android:textColor">@color/textOverBackgrnd</item>
        <item name="android:textSize">@dimen/textSize</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">serif</item>
    </style>

</resources>
